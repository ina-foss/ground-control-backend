[tox]
envlist = py311
isolated_build=True

[testenv]
deps =
    pylint
    flake8
    pytest
    pytest-cov
    pytest-mock
setenv =
    HOME = /tmp

commands =
    pytest --junitxml=junit_{envname}.xml --cov=src --cov-report=html
    pylint src/
    flake8



