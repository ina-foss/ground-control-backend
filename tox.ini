[tox]
envlist = py311
isolated_build=True

[testenv]
deps =
    pylint
    flake8
    pytest
    pytest-cov
    mock
setenv =
    HOME = /tmp

commands =
    pylint
    flake8
    pytest --junitxml=junit_{envname}.xml --cov=ina_ground_control --cov-report=xml

